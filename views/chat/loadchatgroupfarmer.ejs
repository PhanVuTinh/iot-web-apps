<script type="text/javascript" src='/dist/js/function.js'></script>
<script>
   getChatGroupListforFarmer('<%=conf%>','<%=token%>','<%=userid%>',function(data_chatgrouplist){
        var container = $("#chatGroupList");
        data_chatgrouplist.forEach(function(item){
            container.append(
                $("<a >").text(item.chatgroup_name).click(function(){
                    //todo open chat group message
                    newGroupConversation(item.chatgroup_id,item.chatgroup_name,item.conversation_id,item.chatgroupmember_isManager);
                    console.log(item.conversation_id);
                })
            );
        });
    });
</script>